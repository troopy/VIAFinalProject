@model IEnumerable<BusTracker.Models.shape>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            shape_id
        </th>
        <th>
            shape_pt_lat
        </th>
        <th>
            shape_pt_lon
        </th>
        <th>
            shape_pt_sequence
        </th>
        <th>
            shape_dist_traveled
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.shape_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.shape_pt_lat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.shape_pt_lon)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.shape_pt_sequence)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.shape_dist_traveled)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
